import 'package:flutter/material.dart';

class MessaginScreen extends StatelessWidget {
  const MessaginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("Under Development :P"));
  }
}
